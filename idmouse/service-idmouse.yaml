# Source: idmouse
apiVersion: v1
kind: Service
metadata:
  name: idmouse
  namespace: idmouse
  annotations:
    noa.re/deploy-id: 90a9a159b514b346
spec:
  type: ClusterIP
  selector:
    app: idmouse
  ports:
  - name: http
    port: 80
    targetPort: http
