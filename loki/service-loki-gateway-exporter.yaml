# Source: loki
apiVersion: v1
kind: Service
metadata:
  name: loki-gateway-exporter
  namespace: loki
  labels:
    app.kubernetes.io/name: loki
    app.kubernetes.io/instance: loki
    app.kubernetes.io/version: 3.7.4
    app.kubernetes.io/component: gateway
  annotations:
    noa.re/deploy-id: a9c897b71561fb85
spec:
  type: ClusterIP
  ports:
  - name: http-metrics
    port: 4040
    targetPort: http-metrics
    protocol: TCP
  selector:
    app.kubernetes.io/name: loki
    app.kubernetes.io/instance: loki
    app.kubernetes.io/component: gateway
